<%= form_for(@oder) do |f| %>
  <% if @oder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@oder.errors.count, "error") %> prohibited this oder from being saved:</h2>

      <ul>
      <% @oder.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :pay_type %><br>
    <%= f.text_field :pay_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
